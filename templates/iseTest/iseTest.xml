<?xml version="1.0" encoding="UTF-8" standalone="no"?>
<templates>
    <template autoinsert="true" context="java" deleted="false"
              description="Creates a test case for junit 4 which expects an IllegalStateException to be thrown. This template uses underscore naming conventions for tests. iseTest stands for 'Illegal State Exception Test'."
              enabled="true" name="iseTest">@Test(expected = IllegalStateException.class) ${:import(org.junit.Test)}&#13;
        public void test${methodName}_${scenario}_throwsException()&#13;
        {&#13;
        ${cursor}&#13;
        }
    </template>
</templates>